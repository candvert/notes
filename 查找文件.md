使用的是[telescope](https://github.com/nvim-telescope/telescope.nvim)
```lua
require("lazy").setup({
  spec = {
	{
		'nvim-telescope/telescope.nvim',
		dependencies = { 
			'nvim-lua/plenary.nvim',
			'nvim-treesitter/nvim-treesitter'
		},
		-- 设置按键映射，<leader>一般为空格，即查找文件快捷键为 ff
		config = function()
			local builtin = require('telescope.builtin')
			vim.keymap.set('n', '<leader>ff', builtin.find_files, { desc = 'Telescope find files' })
			-- 搜索当前目录下的字符串
			vim.keymap.set('n', '<leader>fg', builtin.live_grep, { desc = 'Telescope live grep' })
			-- 在缓冲区的文件列表中进行搜索
			vim.keymap.set('n', '<leader>fb', builtin.buffers, { desc = 'Telescope buffers' })
			-- 在telescope的帮助手册中进行搜索
			vim.keymap.set('n', '<leader>fh', builtin.help_tags, { desc = 'Telescope help tags' })
		end,
    },
  },
})
```
可以使用 :checkhealth telescope 命令查看安装情况

快捷键：
```
使用:Telescope find_files或快捷键 ff打开搜索界面
下面都是打开搜索界面之后使用的快捷键

Ctrl + n 上一个
Ctrl + p   下一个
j/k          下一个/上一个（普通模式）
H/M/L        选择第一个/中间/最后一个（普通模式）
gg/G         选择第一个/最后一个（普通模式）
<CR>         即Enter键，确认选择
<C-x>        在下方窗口打开文件
<C-t>        在新的窗口打开文件
<C-d>        在文件预览窗口向下滚动
<C-u>        在文件预览窗口向上滚动
<C-/>        显示快捷键及功能（插入模式）
<C-c>        关闭搜索界面（搜索模式）
<Esc>        关闭搜索界面（普通模式）


picker可以认为是弹出的搜索界面，具体一点可以认为是某个函数，比如builtin.find_files，builtin.live_grep
picker非常灵活，支持全局配置和针对单个选择器的配置
```

```lua
require('telescope').setup{
  defaults = {
    -- 在这里填写默认配置:
    -- config_key = value,
    mappings = {
      i = {
        -- 将actions.which_key映射到<C-h>
        -- actions.which_key显示该picker的按键映射
        ["<C-h>"] = "which_key",
		["<esc>"] = require('telescope.actions').close,
		["<C-n>"] = false,
      }
    }
  },
  pickers = {
    -- 内置picker的默认配置写在这里：
    -- picker_name = {
    --   picker_config_key = value,
    --   ...
    -- }
	-- 现在picker_config_key会在你每次调用该内置picker时生效
  },
  extensions = {
    -- 扩展的配置写在这里：
    -- extension_name = {
    --   extension_config_key = value,
    -- }
	-- 请看你想要配置的扩展的readme
  }
}

{opts.defaults}中的key，即默认配置的选项
sorting_strategy                     "descending" (默认)/"ascending"
selection_strategy   每次sort后光标的行为 "reset"(default)/"follow"/"row"/"closest"/"none"
scroll_strategy                      "cycle" (默认)/"limit"
layout_strategy                      默认: 'horizontal'
示例：
layout_config：
        Default: {
          bottom_pane = {
            height = 25,
            preview_cutoff = 120,
            prompt_position = "top"
          },
          center = {
            height = 0.4,
            preview_cutoff = 40,
            prompt_position = "top",
            width = 0.5
          },
          cursor = {
            height = 0.9,
            preview_cutoff = 40,
            width = 0.8
          },
          horizontal = {
            height = 0.9,
            preview_cutoff = 120,
            prompt_position = "bottom",
            width = 0.8
          },
          vertical = {
            height = 0.9,
            preview_cutoff = 40,
            prompt_position = "bottom",
            width = 0.8
          }
        }
dynamic_preview_title   默认: false
prompt_prefix      搜索框的前缀       默认: '> '
selection_caret      当前选中条目前的字符    默认: '> '
entry_prefix    每个搜索条目的前缀，不包括当前选中的条目        默认: '  '
border       边框     默认: true
borderchars  组成边框的字符   默认: { "─", "│", "─", "│", "╭", "╮", "╯", "╰" }
results_title    可以填false to hide the title   默认: "Results"
prompt_title         可以填false to hide the title      默认: "Prompt"


path_display     文件路径如何显示(默认: {})：
"hidden"    hide file names
"tail"      only display the file name, and not the path
"absolute"  display absolute paths
"shorten"  only display the first character of each directory in the path
"truncate"  truncates the start of the path when the whole path will
not fit. To increase the gap between the path and the edge,
set truncate to number `truncate = 3`

path_display.shorten = num
对于alpha/beta/gamma/delta.txt
path_display.shorten = { len = 1, exclude = {1, -1} }  alpha/b/g/delta.txt
path_display.shorten = { len = 2, exclude = {2, -2} }  al/beta/gamma/de
path_display = function(opts, path)
  local tail = require("telescope.utils").path_tail(path)
  return string.format("%s (%s)", tail, path)
end,


mappings           :h telescope.mappings
default_mappings        默认: nil     :h telescope.defaults.mappings
wrap_results      如果文件名太长，则换行    默认: false
initial_mode           默认: "insert"





find_files这个picker函数的函数原型：
builtin.find_files({opts})
Options:
{cwd}           搜索的目录
{hidden}        是否显示隐藏文件
设置快捷键 en，即在配置文件目录进行搜索，方便更改配置文件
vim.keymap.set('n', '<space>en', function()
	require('telescope.builtin').find_files {
		cwd = vim.fn.stdpath('config')
	}
end)



-- 其他的一些picker
builtin.git_files          对 git ls-files 命令的输出进行模糊搜索
builtin.grep_string        Searches for the string under your cursor or selection in your current working directory



使用layout_strategy和layout_config选项设置布局
对于单个picker设置
require('telescope.builtin').find_files({layout_strategy='vertical',layout_config={width=0.5}})
对全局设置
require('telescope').setup({
  defaults = {
    layout_config = {
      vertical = { width = 0.5 }
      -- other layout configuration here
    },
    -- other defaults configuration here
  },
  -- other configuration values here
})


我使用的布局设置
defaults = {
	borderchars = { "─", "│", "─", "│", "┌", "┐", "┘", "└" },
	results_title = false,
	dynamic_preview_title = true,
	mappings = {
		i = {
		
		}
	},
	
	layout_strategy = "vertical",
	layout_config = {
		vertical = {
			preview_height = 0.5,
			preview_cutoff = 20,
			prompt_position = "top",
		},
		-- 将屏幕撑满
		width = { padding = 0 },
		height = { padding = 0 },
	},
},
```