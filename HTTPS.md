```
HTTPS协议通过SSL/TLS加密
HTTPS报文中的任何东西都被加密，包括所有报头和荷载

HTTP的URL由“http://”开头并默认使用80端口，而HTTPS的URL则是由“https://”开头并默认使用443端口

服务器证书包含了服务器的域名（或IP地址）、服务器的公钥、证书的有效期和CA的签名。通常存放在服务器的特定路径（如/etc/ssl/certs/server.crt）
客户端会预装CA根证书（如操作系统或浏览器的信任库）


什么是自签名证书：
由服务器自己生成的证书（未经过CA签发）。浏览器会警告“证书不受信任”，仅用于测试或内部环境
```
